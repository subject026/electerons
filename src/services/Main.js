// const { ipcRenderer } = require("electron");

// export const getUserData = new Promise(async (resolve, reject), () => {
//   // ipcRenderer.on("send-user-data", function (event, store) {
//   //   // console.log(event);
//   // });

//   const userData = await ipcRenderer.invoke("request-user-data");
//   resolve(store);
// });

// export default {
//   getUserData,
// };
